import type { NextPage } from 'next';
import Head from 'next/head';
import Image from 'next/image';

const Home: NextPage = () => {
	return (
		<div>
			<h1>hi</h1>
			<main>
				{/* Sidebar */}
				{/* Center */}
			</main>
			{/* Footer */}
		</div>
	);
};

export default Home;
